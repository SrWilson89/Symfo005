{% extends 'base.html.twig' %}

{% block title %}Tareas{% endblock %}

{% block body %}
    <h1>Tareas</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tarea.id }}</td>
            </tr>
            <tr>
                <th>Notas</th>
                <td>{{ tarea.notas }}</td>
            </tr>
            <tr>
                <th>DateAdd</th>
                <td>{{ tarea.dateAdd ? tarea.dateAdd|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>DateEdit</th>
                <td>{{ tarea.dateEdit ? tarea.dateEdit|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_tareas_index') }}">back to list</a>

    <a href="{{ path('app_tareas_edit', {'id': tarea.id}) }}">edit</a>

    {{ include('tareas/_delete_form.html.twig') }}
{% endblock %}
