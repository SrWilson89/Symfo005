{% extends 'base.html.twig' %}

{% block title %}Edit Estados{% endblock %}

{% block body %}
    <h1>Edit Estados</h1>

    {{ include('estados/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_estados_index') }}">back to list</a>

    {{ include('estados/_delete_form.html.twig') }}
{% endblock %}
