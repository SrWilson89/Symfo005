{% extends 'base.html.twig' %}

{% block title %}Estados{% endblock %}

{% block body %}
    <h1>Estados</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ estado.id }}</td>
            </tr>
            <tr>
                <th>Estado</th>
                <td>{{ estado.estado }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_list', {'entity': 'estados'}) }}">back to list</a>

    <a href="{{ path('app_estados_edit', {'id': estado.id}) }}">edit</a>

    {{ include('estados/_delete_form.html.twig') }}
{% endblock %}