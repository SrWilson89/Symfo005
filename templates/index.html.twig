{# templates/user/index.html.twig #}

{% extends 'base.html.twig' %}

{% block title %}Lista de Usuarios{% endblock %}

{% block body %}
    <h1>Lista de Usuarios</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Email</th>
                <th>Roles</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td>{{ entity.id }}</td>
                <td>{{ entity.email }}</td>
                <td>{{ entity.roles|join(', ') }}</td>
                <td>
                    <a href="{{ path('app_user_edit', {'id': entity.id}) }}">editar</a>
                    <a href="{{ path('app_user_delete', {'id': entity.id}) }}">eliminar</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">No se encontraron usuarios.</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_user_new') }}">Crear nuevo usuario</a>
{% endblock %}