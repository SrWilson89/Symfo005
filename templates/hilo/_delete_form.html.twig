<form method="post" action="{{ path('app_hilo_delete', {'id': hilo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ hilo.id) }}">
    <button class="btn">Delete</button>
</form>
